rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allows public download of player profile cards, locked writes
    match /players/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}